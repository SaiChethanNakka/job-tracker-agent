# Job Tracker Agent — Environment Variables
# Copy this file to .env and fill in your values

# Gemini API key (FREE — get from https://aistudio.google.com -> Get API Key)
GEMINI_API_KEY=AIza...

# Gmail OAuth credentials are handled via credentials.json (downloaded from GCP)
# No Gmail credentials needed here — the OAuth flow uses credentials.json directly
